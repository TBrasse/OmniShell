function Test-CustomSegment {
    param(
        [object] $CustomSegment
    )
    # switch ($CustomSegment) {
    #     { -not ($_ -is [hashtable]) } { throw "this and that" }
    #     { -not $_.Keys -}
    #     Default {}
    # }
}